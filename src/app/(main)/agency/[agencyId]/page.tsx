import AgencyDetails from "@/components/forms/AgencyDetails";
import React from "react"

type Props = {
    params: {
        agencyId: string;
    }
}

const AgencyIDPage = ({params: {agencyId}}: Props) => {
  return (
    <div className="relative h-full">
    </div>
  )
}

export default AgencyIDPage